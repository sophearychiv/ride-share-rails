<h1>Passenger Show View</h1>
<%= @passenger.name%>
<br>
<%= @passenger.phone_num%>
<br>
This is the total cost
<%= @passenger.total_cost %><br>
This is all the trips the passenger went on: <br>
<% @passenger.all_trips.each do |trip|%>
    <%= link_to "#{trip.id}", passenger_trip_path(@passenger.id) %>
    <%= trip.date%>
    <br>
<% end %>

<div class="bottom-links">
<%= link_to "Edit Passenger", edit_passenger_path(@passenger.id) %> <br>
<%= link_to "Delete Passenger", passenger_path(@passenger.id), method: :delete, data: {confirm: "Are you sure you'd like to delete this passenger?"} %> <br>
<%= link_to "Return to All Passengers", passengers_path %> <br>
<%= link_to "Homepage", "/" %>
</div>